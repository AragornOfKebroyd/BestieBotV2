const { Events } = require('discord.js');

module.exports = {
	name: 'xreply',
	execute(message) {
		
		console.log(`message sent ${message.content}`);
	},
};


function determinex(userMessage){
	index = 0
	for (index = 0; index < userMessage.length-1; index++){

	}
}